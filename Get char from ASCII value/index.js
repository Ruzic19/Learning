//instructions :

//Write a function getChar / GetCgar / get_char which takes a number and returns the corresponding ASCII char for that value 

// example : -----> getChar(65) # => 'A'

function getChar(c) {

    return String.fromCharCode(c); //<--- funkcija vec postoji u JS, ali mogli smo napraviti array pa da izvucemo poziciju
    
}